name: Process-DNS-Blocklist  # No spaces

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
  push:
    branches: [ main, master ]  # Added master just in case

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    # ... rest of your workflow
